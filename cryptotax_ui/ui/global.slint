export global Actions {
    // The int parameter refers to the index of the source to change
    callback set_source_enabled(int, bool);

    // The int parameter refers to the index of the report to export
    callback export_summary(int);
    callback export_capital_gains(int);
}
